# Notas:
Este es mi primer programa en Node
 ```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base por la cual voy a multiplicar    [number] [required]
  -l, --listar   Indica si se debe mostrar en consola el resultado
                                                      [boolean] [default: false]
  -h, --hasta    Hasta qué número se calcula la tabla                   [number]
  ```
